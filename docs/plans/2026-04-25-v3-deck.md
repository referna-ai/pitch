# V3 Pitch Deck Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build a v3 pitch deck in `/v3/` with a referrals-first narrative (9 slides: S0–S8), cloned from the root deck and modified slide-by-slide.

**Architecture:** Clone root files into `v3/`, update shared JS files (nav, analytics) for the new slide list, then modify/replace slides individually. Three root slides (slide-3 through slide-5) are replaced with entirely new content; slide-6 and slide-7 shift to slide-7 and slide-8.

**Tech Stack:** Vanilla HTML/CSS/JS. Shared files: `nav.js`, `analytics.js`, `copy-deck.js`, `styles.css`. No build step.

---

## Slide mapping: root → v3

| v3 file | Source | Changes |
|---------|--------|---------|
| `index.html` | root `index.html` | One subtitle text change |
| `slide-1.html` | root `slide-1.html` | Remove one bullet, change conclusion |
| `slide-2.html` | root `slide-2.html` | Renumber to 02 / 08 only |
| `slide-3.html` | NEW | The Gap — $17T referral market |
| `slide-4.html` | NEW | Past Attempts — intros ≠ referrals |
| `slide-5.html` | NEW | The Positioning — 2×2 matrix |
| `slide-6.html` | NEW | InTouch — how referrals scale |
| `slide-7.html` | root `slide-6.html` | Renumber to 07 / 08, update ask/milestone/value |
| `slide-8.html` | root `slide-7.html` | Renumber to 08 / 08 only |

---

## Task 1: Clone root to v3/

**Files:**
- Create: `v3/` directory with all root files

**Step 1: Clone**
```bash
mkdir -p v3
cp index.html slide-1.html slide-2.html slide-3.html slide-4.html \
   slide-5.html slide-6.html slide-7.html \
   nav.js analytics.js copy-deck.js styles.css v3/
```

**Step 2: Create slide-8.html from slide-7.html**
```bash
cp v3/slide-7.html v3/slide-8.html
```

**Step 3: Verify**
```
ls v3/
```
Expected: index.html, slide-1.html through slide-8.html, nav.js, analytics.js, copy-deck.js, styles.css

**Step 4: Commit**
```bash
git add v3/
git commit -m "v3: clone root deck as starting point"
```

---

## Task 2: Update nav.js for v3 slide list

**Files:**
- Modify: `v3/nav.js` lines 5–14

The v3 slide order has 9 entries (index + 8 slides). Replace the `slides` array:

**Step 1: Edit `v3/nav.js` — update the slides array**
```javascript
const slides = [
  'index.html',
  'slide-1.html',
  'slide-2.html',
  'slide-3.html',
  'slide-4.html',
  'slide-5.html',
  'slide-6.html',
  'slide-7.html',
  'slide-8.html'
];
```

**Step 2: Verify** — open `v3/index.html` in browser, confirm arrow navigation cycles through 9 positions.

**Step 3: Commit**
```bash
git add v3/nav.js
git commit -m "v3: update nav.js for 9-slide order"
```

---

## Task 3: Update analytics.js for v3 slide names

**Files:**
- Modify: `v3/analytics.js` — `SLIDES` map (lines 10–19)

**Step 1: Edit `v3/analytics.js` — replace SLIDES map**
```javascript
var SLIDES = {
  'index.html':   '00 · Cover',
  'slide-1.html': '01 · The Networkers',
  'slide-2.html': '02 · The Trend',
  'slide-3.html': '03 · The Gap',
  'slide-4.html': '04 · Past Attempts',
  'slide-5.html': '05 · The Positioning',
  'slide-6.html': '06 · InTouch',
  'slide-7.html': '07 · The Plan',
  'slide-8.html': '08 · The Team'
};
```

**Step 2: Commit**
```bash
git add v3/analytics.js
git commit -m "v3: update analytics slide map"
```

---

## Task 4: Update nav tabs in every slide file

Every slide has a `<nav class="deck-tabs">` block. All 9 files need tabs for all 9 slides. Also update `data-slide` and `slide-number` in each file.

**Files:** `v3/index.html`, `v3/slide-1.html` through `v3/slide-8.html`

The nav block to use in every file (mark the current slide `active`):
```html
<nav class="deck-tabs">
  <a href="index.html" class="deck-tab">Overview</a>
  <a href="slide-1.html" class="deck-tab">The networkers</a>
  <a href="slide-2.html" class="deck-tab">The trend</a>
  <a href="slide-3.html" class="deck-tab">The gap</a>
  <a href="slide-4.html" class="deck-tab">Past attempts</a>
  <a href="slide-5.html" class="deck-tab">The positioning</a>
  <a href="slide-6.html" class="deck-tab">InTouch</a>
  <a href="slide-7.html" class="deck-tab">The plan</a>
  <a href="slide-8.html" class="deck-tab">The team</a>
</nav>
```

**Step 1: Update `v3/index.html`**
- Nav tabs: full list above, `index.html` tab gets `active`
- No slide-number div on index

**Step 2: Update `v3/slide-1.html`**
- Nav tabs: full list, `slide-1.html` tab gets `active`
- `data-slide="slide-1"` ← already correct
- `slide-number`: `01 / 08`

**Step 3: Update `v3/slide-2.html`**
- Nav tabs: full list, `slide-2.html` active
- `data-slide="slide-2"` ← already correct
- `slide-number`: `02 / 08`

**Step 4: Update `v3/slide-3.html`** (will be replaced in Task 7, but set the shell now)
- Nav tabs: full list, `slide-3.html` active
- `data-slide="slide-3"` ← already correct
- `slide-number`: `03 / 08`

**Step 5: Update `v3/slide-4.html`** (will be replaced in Task 8)
- Nav tabs: full list, `slide-4.html` active
- `data-slide="slide-4"` ← already correct
- `slide-number`: `04 / 08`

**Step 6: Update `v3/slide-5.html`** (will be replaced in Task 9)
- Nav tabs: full list, `slide-5.html` active
- `data-slide="slide-5"` ← already correct
- `slide-number`: `05 / 08`

**Step 7: Update `v3/slide-6.html`** (will be replaced in Task 10)
- Nav tabs: full list, `slide-6.html` active
- `data-slide="slide-6"` ← already correct
- `slide-number`: `06 / 08`

**Step 8: Update `v3/slide-7.html`** (cloned from root slide-6 / The Plan)
- Nav tabs: full list, `slide-7.html` active
- Change `data-slide="slide-6"` → `data-slide="slide-7"`
- `slide-number`: `07 / 08`

**Step 9: Update `v3/slide-8.html`** (cloned from root slide-7 / The Team)
- Nav tabs: full list, `slide-8.html` active
- Change `data-slide="slide-7"` → `data-slide="slide-8"`
- `slide-number`: `08 / 08`

**Step 10: Commit**
```bash
git add v3/
git commit -m "v3: update nav tabs, data-slide, slide numbers in all 9 files"
```

---

## Task 5: Update index.html (S0 — Title)

**Files:**
- Modify: `v3/index.html`

One text change only. Find the subtitle paragraph and update:

**Old:**
```
Built for the 338 million professionals in small teams who run on who they know, and the agents that will soon run alongside them.
```

**New:**
```
Built for 338 million professionals who run on referrals.
```

Everything else (logo, tagline "The trust network", "Pre-Seed Round" kicker) stays identical.

**Step 1: Make the text change in `v3/index.html`**

**Step 2: Verify** — open `v3/index.html` in browser, confirm subtitle reads correctly.

**Step 3: Commit**
```bash
git add v3/index.html
git commit -m "v3 S0: update subtitle to referrals-first"
```

---

## Task 6: Update slide-1.html (S1 — The Networkers)

**Files:**
- Modify: `v3/slide-1.html`

**Step 1: Remove the third bullet** (the "2× more active on LinkedIn" bullet)

Keep only:
```html
<ul class="bullets">
  <li>82% of small teams say referrals are their primary source of new customers</li>
  <li>81% already spend 1+ hours/week trying to scale their network — and get poor results</li>
</ul>
```

**Step 2: Update the conclusion**

Find `<div class="conclusion">` and change to:
```html
<div class="conclusion">→ A venture-scale problem with no product category.</div>
```

**Step 3: Keep hero stats (338M, 35 Billion hours/year) and sources unchanged.**

**Step 4: Verify** — open `v3/slide-1.html`, confirm 2 bullets, new conclusion.

**Step 5: Commit**
```bash
git add v3/slide-1.html
git commit -m "v3 S1: remove third bullet, update conclusion"
```

---

## Task 7: Build slide-3.html (S3 — The Gap)

**Files:**
- Replace: `v3/slide-3.html` (currently a clone of root slide-3 / The Network)

Use the exact same HTML shell as other slides (copy from `v3/slide-2.html` as the template — same `<head>`, same `<nav>`, same `<script src="nav.js">`). Replace only the `.slide` content block.

**Step 1: Copy slide-2.html as the shell**
```bash
cp v3/slide-2.html v3/slide-3.html
```
Then set `data-slide="slide-3"`, slide-number `03 / 08`, tab `slide-3.html` active.

**Step 2: Replace the `.slide` content**

```html
<div class="slide">
  <div class="slide-number">03 / 08</div>
  <div class="slide-label">The Gap</div>
  <h2 class="slide-title">$17 trillion in referrals. No software.</h2>

  <ul class="bullets">
    <li>65% of small team revenue comes from referrals. That's $17 trillion globally — running on memory, favors, and breakfast meetings.</li>
  </ul>

  <div class="gap-analogy">
    <div class="gap-analogy-row">
      <span class="gap-category">Code editor</span>
      <span class="gap-arrow">→</span>
      <span class="gap-winner">Cursor <span class="gap-val">($9B)</span></span>
    </div>
    <div class="gap-analogy-row">
      <span class="gap-category">Search</span>
      <span class="gap-arrow">→</span>
      <span class="gap-winner">Perplexity <span class="gap-val">($20B)</span></span>
    </div>
    <div class="gap-analogy-row">
      <span class="gap-category">Customer support</span>
      <span class="gap-arrow">→</span>
      <span class="gap-winner">Sierra <span class="gap-val">($10B)</span></span>
    </div>
    <div class="gap-analogy-row gap-analogy-empty">
      <span class="gap-category">Professional referrals</span>
      <span class="gap-arrow">→</span>
      <span class="gap-winner gap-unknown">???</span>
    </div>
  </div>

  <div class="conclusion">→ AI has created a new winning answer in every professional category — except the one that drives 65% of revenue.</div>
  <div class="sources">Sources: DemandSage (65% stat), Anysphere/Cursor, Perplexity, Sierra AI — public valuations</div>
</div>
```

**Step 3: Add CSS for gap analogy** (add to the `<style>` block in `v3/slide-3.html`, or better — add to `v3/styles.css` so it's shared)

Add to `v3/styles.css`:
```css
.gap-analogy {
  margin: 28px 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.gap-analogy-row {
  display: flex;
  align-items: center;
  gap: 16px;
  font-size: 17px;
  color: var(--gray-mid);
}
.gap-category { min-width: 200px; }
.gap-arrow { color: var(--gold); font-weight: 700; }
.gap-winner { color: var(--white); font-weight: 600; }
.gap-val { color: var(--gray-mid); font-weight: 400; font-size: 14px; }
.gap-analogy-empty {
  margin-top: 12px;
  padding-top: 16px;
  border-top: 1px solid var(--border);
  font-size: 20px;
}
.gap-unknown {
  color: var(--gold) !important;
  font-size: 24px;
  font-weight: 800;
}
```

**Step 4: Verify** — open `v3/slide-3.html`, confirm layout, gold `???`, nav works.

**Step 5: Commit**
```bash
git add v3/slide-3.html v3/styles.css
git commit -m "v3 S3: The Gap slide — \$17T referrals, AI category analogy"
```

---

## Task 8: Build slide-4.html (S4 — Past Attempts)

**Files:**
- Replace: `v3/slide-4.html`

**Step 1: Copy shell from slide-2.html**
```bash
cp v3/slide-2.html v3/slide-4.html
```
Set `data-slide="slide-4"`, slide-number `04 / 08`, tab `slide-4.html` active.

**Step 2: Replace `.slide` content**

```html
<div class="slide">
  <div class="slide-number">04 / 08</div>
  <div class="slide-label">The Attempts</div>
  <h2 class="slide-title">Intros ≠ referrals</h2>

  <p class="slide-lead">Every networking startup tried to create new connections.<br>None of them activated existing trust.</p>

  <div class="attempts-grid">
    <div class="attempts-col">
      <div class="attempts-col-label">What they built</div>
      <ul class="bullets">
        <li><strong>Lunchclub</strong> — AI-matched strangers for coffee</li>
        <li><strong>Shapr</strong> — Tinder-style professional swiping</li>
        <li><strong>LinkedIn</strong> — 930 connections, mostly forgotten</li>
      </ul>
    </div>
    <div class="attempts-col attempts-col-right">
      <div class="attempts-col-label">What's actually valuable</div>
      <ul class="bullets">
        <li>A circle of 8 people who agreed to look out for each other</li>
        <li>Who scan every meeting for your needs</li>
        <li>Who refer you when you're not in the room</li>
      </ul>
    </div>
  </div>

  <p class="attempts-bridge">The difference: introductions connect you to someone new. Referrals come from someone who already trusts you — and knows exactly what you need.</p>

  <div class="conclusion">→ The winning model already exists. It just hasn't been digitized.</div>
  <div class="sources">Sources: Lunchclub (a16z-backed, pivoted), Shapr (declined), InTouch interviews (n=26)</div>
</div>
```

**Step 3: Add CSS to `v3/styles.css`**

```css
.slide-lead {
  color: var(--gray-mid);
  font-size: 16px;
  line-height: 1.6;
  margin-bottom: 24px;
}
.attempts-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 32px;
  margin-bottom: 24px;
}
.attempts-col-label {
  font-size: 11px;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--gold);
  margin-bottom: 12px;
  font-weight: 700;
}
.attempts-col-right .attempts-col-label { color: var(--gray-mid); }
.attempts-bridge {
  font-size: 15px;
  color: var(--gray-mid);
  border-left: 2px solid var(--border);
  padding-left: 16px;
  margin-bottom: 24px;
  line-height: 1.6;
}
@media (max-width: 768px) {
  .attempts-grid { grid-template-columns: 1fr; }
}
```

**Step 4: Verify** — open `v3/slide-4.html`, confirm two-column layout, responsive on mobile.

**Step 5: Commit**
```bash
git add v3/slide-4.html v3/styles.css
git commit -m "v3 S4: Past Attempts slide — intros vs referrals"
```

---

## Task 9: Build slide-5.html (S5 — The Positioning 2×2)

**Files:**
- Replace: `v3/slide-5.html`

**Step 1: Copy shell from slide-2.html**
```bash
cp v3/slide-2.html v3/slide-5.html
```
Set `data-slide="slide-5"`, slide-number `05 / 08`, tab `slide-5.html` active.

**Step 2: Replace `.slide` content**

```html
<div class="slide">
  <div class="slide-number">05 / 08</div>
  <div class="slide-label">The Positioning</div>
  <h2 class="slide-title">Where InTouch sits</h2>

  <div class="matrix-wrap">
    <div class="matrix-yaxis">Trust / Referrals <span class="matrix-yaxis-low">Volume / Reach</span></div>
    <div class="matrix-body">
      <div class="matrix-xaxis">
        <span>Analog</span>
        <span>Digital</span>
      </div>
      <div class="matrix-grid">
        <div class="matrix-cell matrix-cell-muted">
          <div class="matrix-cell-label">Job boards, classifieds</div>
        </div>
        <div class="matrix-cell">
          <div class="matrix-cell-label">LinkedIn</div>
          <div class="matrix-cell-sub">$26B acquisition</div>
        </div>
        <div class="matrix-cell">
          <div class="matrix-cell-label">BNI</div>
          <div class="matrix-cell-sub">$26.4B in referrals · 340K members · 40 years old</div>
        </div>
        <div class="matrix-cell matrix-cell-gold">
          <div class="matrix-cell-label">InTouch</div>
          <div class="matrix-cell-sub">Digital · AI-amplified · circle-based</div>
        </div>
      </div>
    </div>
  </div>

  <p class="matrix-stat">BNI members: 79% are in companies under 10 people. Average member generates $62K/year from referrals.</p>

  <div class="conclusion">→ LinkedIn digitized reach. InTouch digitizes trust.</div>
  <div class="sources">Sources: BNI Mid-Year Report 2025 (n=340K), BNI UK&amp;Ireland survey (n=4,785), Microsoft/LinkedIn acquisition ($26.2B, 2016)</div>
</div>
```

**Step 3: Add CSS to `v3/styles.css`**

```css
.matrix-wrap {
  display: flex;
  gap: 8px;
  margin: 24px 0;
  align-items: stretch;
}
.matrix-yaxis {
  writing-mode: vertical-rl;
  transform: rotate(180deg);
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: .1em;
  color: var(--gray-mid);
  display: flex;
  justify-content: space-between;
  padding: 4px 0;
  min-width: 20px;
}
.matrix-yaxis-low { color: var(--gray-dark, #555); }
.matrix-body { flex: 1; }
.matrix-xaxis {
  display: flex;
  justify-content: space-around;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: .1em;
  color: var(--gray-mid);
  margin-bottom: 8px;
}
.matrix-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: 8px;
}
.matrix-cell {
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 18px;
  min-height: 90px;
}
.matrix-cell-muted { opacity: .35; }
.matrix-cell-gold {
  border-color: var(--gold);
  background: rgba(212, 165, 116, .06);
}
.matrix-cell-label { font-weight: 700; font-size: 15px; margin-bottom: 4px; }
.matrix-cell-sub { font-size: 12px; color: var(--gray-mid); line-height: 1.5; }
.matrix-stat {
  font-size: 13px;
  color: var(--gray-mid);
  margin-bottom: 20px;
  font-style: italic;
}
@media (max-width: 768px) {
  .matrix-wrap { flex-direction: column; }
  .matrix-yaxis { writing-mode: horizontal-tb; transform: none; flex-direction: row; justify-content: space-between; }
}
```

**Step 4: Verify** — open `v3/slide-5.html`, confirm 2×2 grid, InTouch cell has gold border, muted top-left cell.

**Step 5: Commit**
```bash
git add v3/slide-5.html v3/styles.css
git commit -m "v3 S5: The Positioning — 2x2 matrix"
```

---

## Task 10: Build slide-6.html (S6 — InTouch / How it works)

**Files:**
- Replace: `v3/slide-6.html`

**Step 1: Copy shell from slide-2.html**
```bash
cp v3/slide-2.html v3/slide-6.html
```
Set `data-slide="slide-6"`, slide-number `06 / 08`, tab `slide-6.html` active.

**Step 2: Replace `.slide` content**

```html
<div class="slide">
  <div class="slide-number">06 / 08</div>
  <div class="slide-label">The Network</div>
  <h2 class="slide-title">How referrals actually scale</h2>

  <div class="how-steps">
    <div class="how-step">
      <div class="how-step-num">1</div>
      <div class="how-step-body">
        <div class="how-step-title">Connect your calendar</div>
        <div class="how-step-desc">InTouch maps who you actually meet — verified by interaction history. Not who clicked "accept" three years ago.</div>
      </div>
    </div>
    <div class="how-step">
      <div class="how-step-num">2</div>
      <div class="how-step-body">
        <div class="how-step-title">Build your circle</div>
        <div class="how-step-desc">3–8 people who agree to actively look out for each other. Everyone shares what they need. Everyone scans their meetings.</div>
      </div>
    </div>
    <div class="how-step">
      <div class="how-step-num">3</div>
      <div class="how-step-body">
        <div class="how-step-title">Referrals surface automatically</div>
        <div class="how-step-desc">7.04 warm paths per week — people and opportunities you'd never find alone. Each worth ~$1,200 on average (BNI, n=340K).</div>
      </div>
    </div>
  </div>

  <div class="conclusion">→ 68 real connections. 7 referrals a week. Built on trust you already earned.</div>
  <div class="sources">Sources: InTouch product data, BNI/Misner (n=12,000)</div>
</div>
```

**Step 3: Add CSS to `v3/styles.css`**

```css
.how-steps {
  display: flex;
  flex-direction: column;
  gap: 20px;
  margin: 24px 0;
}
.how-step {
  display: flex;
  gap: 20px;
  align-items: flex-start;
}
.how-step-num {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: 1px solid var(--gold);
  color: var(--gold);
  font-weight: 800;
  font-size: 15px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.how-step-title {
  font-weight: 700;
  font-size: 16px;
  margin-bottom: 4px;
}
.how-step-desc {
  font-size: 14px;
  color: var(--gray-mid);
  line-height: 1.6;
}
```

**Step 4: Verify** — open `v3/slide-6.html`, confirm 3 numbered steps, gold circles.

**Step 5: Commit**
```bash
git add v3/slide-6.html v3/styles.css
git commit -m "v3 S6: InTouch product slide — 3-step how it works"
```

---

## Task 11: Update slide-7.html (S7 — The Plan)

**Files:**
- Modify: `v3/slide-7.html` (was cloned from root `slide-6.html` / The Plan)

This slide is cloned correctly — just update the plan box content and slide number.

**Step 1: Confirm slide-number is `07 / 08`** (done in Task 4)

**Step 2: Find the plan box and ensure it contains:**
```
ASK        Pre-seed round — 24 months, team of 4
MILESTONE  4,000 WAU · 4,000 weekly intros · K = 1.3/month
VALUE      Each intro worth ~$1,200 (BNI, n=340K) → $250M/year flowing through the network
```

**Step 3: Ensure side notes include $250M/year derivation:**
```
$250M/year = 4,000 WAU × 7.04 intros/week × $1,200/intro × 7.4 network effect
```
(Match whatever derivation format the existing plan slide uses for its other side notes.)

**Step 4: Verify** — open `v3/slide-7.html`, confirm plan box and side notes.

**Step 5: Commit**
```bash
git add v3/slide-7.html
git commit -m "v3 S7: The Plan — update ask/milestone/value, renumber 07/08"
```

---

## Task 12: Verify full navigation flow

**Step 1:** Open `v3/index.html` in browser.

**Step 2:** Click/arrow through all 9 slides in order (S0 → S8). Confirm:
- Each slide loads correctly
- Slide numbers read `00/08` through `08/08` (index has no number)
- Nav tabs highlight the correct active tab
- Back/forward arrows appear correctly (no arrow on first/last)

**Step 3:** Confirm analytics fires — open DevTools Network tab, verify POST to the webhook URL on each slide.

**Step 4:** Confirm copy button on index.html collects all 8 slide `.slide` elements (not index itself).

**Step 5: Final commit**
```bash
git add v3/
git commit -m "v3: full navigation and analytics verified"
```

---

## Task 13: Open PR

**Step 1:**
```bash
git checkout -b v3-pitch-deck
git push -u origin v3-pitch-deck
gh pr create --title "V3 pitch deck — referrals-first narrative" \
  --body "New /v3/ deck with 9 slides. Referrals-first story replacing AI agent narrative. All existing decks (root, /v2/) untouched."
```
